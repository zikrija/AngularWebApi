(function () {
    "use strict";
    // this is global module
    var app = angular.module("productManagement",
                            ["common.services"]);// here we define module dependency, this depends on new module common.services

}());